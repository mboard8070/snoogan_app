# === Workbench generated files ===
.project/*
!.project/spec.yaml
!.project/configpacks

# === macOS / OS junk ===
.DS_Store
Thumbs.db

# === Editor / IDE ===
.vscode/
.idea/
*.swp
*~
*.sublime-workspace
*.sublime-project

# === Python / venv ===
__pycache__/
*.py[cod]
*$py.class
*.so
venv/
env/
.venv/
ENV/

# === Streamlit specific ===
.streamlit/
streamlit_config.toml
.streamlit/secrets.toml

# Vector DB – local embeddings for the RAG brain (never commit)
data/vector_db/
!data/vector_db/.gitkeep  # Optional: keep folder tracked empty if you want
# === Environment & secrets (CRITICAL — never commit) ===
.env
variables.env
*.key
*.pem
secrets/
config/*.key

# === Logs & runtime ===
*.log
nohup.out
dashboard.log

# === Data / temp files ===
*.csv
*.pkl
*.sqlite3
*.db
trade_history.csv
logs/

# === Local scripts ===
*.sh
start-dashboard.sh
stop-dashboard.sh

# === Jupyter ===
.ipynb_checkpoints/
*.ipynb

# === Misc ===
*.bak
*.tmp

# === SNOOGANS APP SPECIFIC ===

# Strategy persistence & state files (contain live positions, PnL, equity history)
strategy_state.json
strategy_15m_state.json
positions.json
positions_15m.json
.current_position.json
daily_pnl.json
equity_history.json
completed_trades.json

# Discord daily flag files (temporary, reset each day)
.greeting_sent_today
.pivots_sent_today
.eod_summary_sent_today

# RAG knowledge directory – contains all closed trade batches for the AI
# (We keep the folder itself in repo if needed, but never the contents)
data/knowledge/
!data/knowledge/.gitkeep  # Optional: add a .gitkeep if you want an empty folder tracked

# End of .gitignore
.Trash-1000/info/test_params.ipynb.trashinfo
.Trash-1000/info/Untitled.ipynb.trashinfo
data/vector_db/fe0b045e-77cb-4240-8550-c1ae03f86a3a/length.bin

# Ignore generated or temporary files managed by the Workbench

# General ignores

# Byte-compiled / optimized / DLL files

# Temp directories, notebooks created by jupyterlab
.ipynb_checkpoints
.Trash-*/
.jupyter/
.local/

# Python distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Workbench Project Layout
data/scratch/*
!data/scratch/.gitkeep
code/data/knowledge/trades.json
code/scalp_strategy_state.json
code/trader/scalp_strategy_state.json